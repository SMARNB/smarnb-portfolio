fastapi>=0.115
uvicorn[standard]>=0.30
SQLAlchemy>=2.0
PyJWT>=2.8
pydantic[email]>=2.7
cryptography>=42.0
python-multipart>=0.0.9
# For production Postgres (e.g. free Neon DB), also install a pure-python driver:
# pg8000>=1.31
# Optional — uncomment to turn on Stripe card payments (then set STRIPE_SECRET_KEY):
# stripe>=9.0
